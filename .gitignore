# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Editor
.vscode
.claude
CONTEXT.md
CLAUDE.md

# Scratch test files (stray root-level test scripts), but not the real
# committed test suite in /tests/
test*
!/tests/
!/tests/**

# Superpowers SDD scratch (local only)
.superpowers

# Worktrees (local scratch, not part of repo)
.worktrees
# Real-world atlas fixtures for the pixel-oracle harness (local scratch data)
.workspaces

# Pulled fresh at build/dev time -- never committed.
www/js/vendor/spine-skeleton-binary
